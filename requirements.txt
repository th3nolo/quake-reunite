fastapi>=0.110
uvicorn[standard]>=0.29
PyYAML>=6.0
requests>=2.31
pypdf>=4.0
python-docx>=1.1
# Video transcription deps live in requirements-video.txt (only needed when the
# `video` source is enabled). Kept out of the base image to keep it small.
